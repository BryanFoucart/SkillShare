{
  "name": "shareskill",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "saas": "sass --style=compressed --watch styles:public/assets/styles",
    "server": "nodemon server.js",
    "browser-sync": "browser-sync start --proxy 'localhost:3000' --files \"public/assets/styles/**/*.css,public/assets/js/**/*.js,views/**/*.ejs\"",
    "start": "concurrently \"npm run saas\" \"npm run server\" \"npm run browser-sync\"" 
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "nodemailer": "^7.0.3"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.10",
    "sass": "^1.89.1"
  }
}
